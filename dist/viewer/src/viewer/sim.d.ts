import { SimulationRuntime } from '../../../src/core/runtime.js';
import type { CounterfactualComparison, WorldSnapshot } from '../../../src/core/types.js';
export declare class ViewerSimulation {
    runtime: SimulationRuntime;
    running: boolean;
    speed: number;
    private accumulator;
    private historySnapshots;
    private lastRecordTime;
    scrubbedSnapshot?: WorldSnapshot;
    constructor();
    private makeWorld;
    reset(): void;
    breakPipe(pipeId?: string, rateKgS?: number): void;
    ruptureFeedLine(rateKgS?: number): void;
    tripPump(): void;
    igniteFire(origin?: {
        x: number;
        y: number;
        z: number;
    }, intensityMw?: number): void;
    igniteTankFire(tankId?: string): void;
    closeESV(): void;
    openESV(): void;
    throttleValve(valveId?: string, openFraction?: number): void;
    triggerDeluge(targetId?: string): void;
    evacuateWorkers(): void;
    triggerFlare(rateKgS?: number): void;
    extinguishAll(): void;
    shiftWind(windX: number, windZ: number): void;
    private historyFrames;
    private recordHistory;
    getHistory(): WorldSnapshot[];
    scrubTo(timeSec: number): WorldSnapshot | undefined;
    stopScrubbing(): void;
    resumeFromScrubbed(): boolean;
    update(realDt: number): void;
    snapshot(): WorldSnapshot;
    evaluateCounterfactual(interventionName: string, applyIntervention: (sim: SimulationRuntime) => void, horizonSec?: number): CounterfactualComparison;
}
